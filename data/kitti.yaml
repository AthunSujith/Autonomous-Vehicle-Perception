# data/kitti.yaml
#$Why: Ultralytics expects a YAML pointing to train/val folders and class names.
# data/kitti.yaml
train: processed/images/train
val:   processed/images/val
nc: 3
names: ['Car','Pedestrian','Cyclist']

